<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.polsl.project.catalogex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".MainScreen"
            android:theme="@style/MainScreenTheme" />
        <activity
            android:name=".CreditsScreen"
            android:label="@string/credits_title"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CreateTemplateScreen"
            android:label="@string/title_activity_create_template_screen"
            android:theme="@style/AppTheme">

        </activity>
        <activity android:name=".CreateCategoryScreen" />
        <activity
            android:name=".categoryElementListScreen"
            android:label="@string/title_activity_category_element_list_screen"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>